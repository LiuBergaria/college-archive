import 'package:flutter/material.dart';
import './ui/listview_pessoa.dart';

void main() => runApp(
      MaterialApp(
        title: 'Carregando dados',
        home: ListViewPessoa(),
      ),
    );
